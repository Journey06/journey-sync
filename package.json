{
  "name": "journey-sync-server",
  "version": "1.0.0",
  "private": true,
  "description": "Journey workbench + auto cross-device sync backend (zero dependencies)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
